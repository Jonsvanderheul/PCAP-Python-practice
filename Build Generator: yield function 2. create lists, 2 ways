#Build Generator: yield function 2. create lists, 2 ways

# def powersOf2(n):
#    pow = 1
#    for i in range(n):
#       yield pow
#        pow *= 2

# t = list(powersOf2(3))

# print(t)

# or

def powersOf2(n):
    pow = 1
    for i in range(n):
        yield pow
        pow *= 2

t = [x for x in powersOf2(5)]

print(t)


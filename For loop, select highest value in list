myList = [17, 3, 11, 5, 1, 9, 7, 15, 33]
largest = myList[0]         # you can start anywher you like and make a program that uses limited lists
                             # i doesn't have te be declared here, it is declared in the for loop itself
for i in range(len(myList)): #could also be written as: for i in range(1,len(myList))
    if myList[i] > largest:
        largest = myList[i]

print(largest)
